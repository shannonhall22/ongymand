<%= simple_form_for(@gym_feature) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :gym_id %>
    <%= f.input :feature_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
